publishing {
    repositories {
        maven {
            name = "testMaven"
            url = "${rootProject.buildDir}/localMaven"
        }
    }
}

afterEvaluate {
    publishing.publications.every { println(it.name) }
}